import { FC, PropsWithChildren } from "react";

interface $name$Props {}

const $name$: FC<PropsWithChildren<$name$Props>> = ({}) => {
  return (
    <div className="$name$" data-testid="$name$">
      $name$ Component
    </div>
  );
};

export default $name$;
